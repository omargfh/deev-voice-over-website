const DeevLogo = (props) => {
    return (
        <div className="logo">
            <img src={`${process.env.PUBLIC_URL}/img/logofull.png`} className="logoImage" />
        </div>
    )
}

export default DeevLogo